services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: zauber
      POSTGRES_PASSWORD: zauber
      POSTGRES_DB: zauber
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: